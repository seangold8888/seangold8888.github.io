# 공주옷장 분류 아이콘

- 공주·머리·드레스·신발·왕관·목걸이·손·날개·친구·배경의 기본 이모지를 전용 SVG 아이콘 10종으로 교체했다.
- 에나멜 장식 같은 둥근 외곽선과 공통 조명, 금속·진주·새틴 색을 사용한다. 아이콘은 34px, 바탕은 38px이며 기존 한글 이름은 유지한다.
- 반복 렌더링 시 그라데이션 ID가 겹치지 않는다. 장식 아이콘은 스크린리더에서 제외하고 버튼에 선택 상태를 제공한다.
- 별도 그림 다운로드나 폰트에 의존하지 않는다. icons.js?v=1을 오프라인 캐시 목록에 추가했다.
- 폰 세로의 그리드 최소 폭 때문에 전체 화면이 옆으로 밀리던 문제를 수정했다. 분류 메뉴만 가로 스크롤된다. 선택 그림은 최소 126px 행을 유지하고 세로 스크롤된다.
- 얼굴·코디·사진·해금과 게임 동작은 바꾸지 않았다. 헤어 보정 studio.js?v=39, 카드 자산 v63도 그대로다. 서비스워커만 v111로 갱신했다.

## 검증

- `node --test "princess/tests/*.test.js" "cards/tests/pwa.test.js" "cards/tests/hub-english-feedback.test.js"`: 56개 통과.
- 로컬 미리보기 서버 실행 후 `node princess/tests/icons-smoke.cjs`: 1180×820, 820×1180, 390×844 통과. 10개 분류 전환, 선택 상태, 44px 이상 터치 영역, 수평 넘침 없음, 선택 그림 최소 높이, 콘솔 오류 없음 확인.
- 최종 캡처: `C:/Users/김시현/AppData/Local/Temp/princess-icons-Y9KW4H/`.
- 로컬 검증만 수행했으며 공개 배포하지 않았다.
