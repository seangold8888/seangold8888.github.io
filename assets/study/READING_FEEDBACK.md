# 영어 문장 읽기 — 칭찬 음성 + 틀린 단어 읽어주기 (스펙 v1)

> 작성: 클로드(기획) 2026-09-06 → 구현: 코덱스. 대상 파일 `assets/study/english-reading.js`, `index.html`(허브), `sw.js`.
> 사용자 확정 원칙: **읽기 연습이 목적. 「먼저 들어 보기」 버튼은 두지 않는다. 문장 전체를 모범 낭독해 주지도 않는다.** 아이가 먼저 읽고, 결과에 따라서만 소리가 난다.

## 1. 통과 시 — 텐션 높은 영어 칭찬 (음성 + 화면)

- 음성 파일: `assets/study/praise/*.mp3` 9개 (Gemini TTS, Leda, 일반 8개 1.06~1.65초, 3연속 3.06초, 총 70KB). 재생은 HTMLAudio. 외부 합성음(speechSynthesis)으로 칭찬하지 않는다 — 텐션이 안 산다.
- 단계:
  | 상황 | 후보 클립 | 화면 문구 |
  |---|---|---|
  | 첫 시도에 통과 | excellent, perfect, awesome, wonderful | 큰 글씨 `⭐ Excellent!` + 작은 한글 `한 번에 읽었어!` |
  | 다시 읽기 후 통과 | great, verygood, youdidit, super | `⭐ Great!` + `다시 읽어서 해냈어!` |
  | 첫 시도 통과 3연속 | threeinarow (전용) | `🌟 Three in a row!` + `세 문장 연속!` + 별 폭죽 |
- 같은 단계 안에서 무작위, 단 **직전 클립과 같은 것은 금지**(세션 메모리로 충분).
- 3연속 카운터는 세션 메모리만(localStorage 저장 안 함). 첫 시도 실패 또는 마이크 오류 시 0으로 리셋. 마이크 오류는 오답이 아니지만 연속 흐름은 끊긴 것으로 본다.
- 순서: 인식기 stop → 화면 문구/별 → 클립 재생 → 재생 끝 후 다음 문제. 일반 칭찬은 최대 1.8초, 총 연출 2초 이내. **3연속 전용 클립은 사용자 확정(2026-09-06)에 따라 길이 제한 없이 끝까지 재생한다**(현재 파일 3.06초). 재생 진행이 5초 동안 없으면 오류 복구로 넘어간다. 기존 통과 처리(`onPass` → `pick`)의 추가 대기는 없앤다.
- 클립 재생 실패(오프라인 캐시 누락, 자동재생 차단)는 조용히 무시. 화면 문구만으로도 성립해야 한다.

## 2. 실패 시 — 틀린 단어만 한 번 읽어주기

- 지금 `finishAttempt`가 빨간 표시하는 그 단어들(`retry` 플래그)을 **문장 순서대로, 최대 3개, 한 번만** 읽어준다. 문장 전체는 읽지 않는다.
- 음성: `speechSynthesis`, `lang = "en-US"`, `rate 0.8`, `pitch 1.0`. 단어 사이 0.4초 간격(utterance 분리). voice 선택: `en-US`이면서 `localService`인 것 우선(아이패드 Samantha). 영어 voice가 하나도 없으면 소리 없이 넘어간다(오류 문구 금지).
- 화면 문구: `"이렇게 읽어요 👂 apples"` — 현재 "문장이 다르게 들렸어요…" 문구를 이 형태로 바꾼다. **"틀렸어요"라는 말은 쓰지 않는다**(인식기 오류인 경우가 많다).
- 읽어주는 동안 빨간 단어를 하나씩 순서대로 노란 하이라이트(듣는 단어 = 보는 단어).
- 예외: `wholeLine`(단어는 다 있는데 딴 말이 섞임)일 때는 아무 단어도 읽어주지 않고 `"문장에 있는 말만 읽어 주세요"`만 표시.
- 재생이 끝나야 `🎤 다시 읽기` 활성. 재생 중 탭하면 speechSynthesis.cancel() 후 바로 녹음 시작.

## 3. 기술 주의 (필수)

1. 소리 내기 전에 인식기 반드시 `stop()`. 소리 끝난 뒤 **자동 재시작 금지** — 스피커 소리를 마이크가 주워서 오답 처리되는 사고 방지. 아이가 버튼을 다시 눌러야 한다.
2. iOS Safari는 사용자 탭 안에서 첫 재생을 풀어야 한다: `🎤 읽어 보기` 탭 핸들러에서 `speechSynthesis.speak(new SpeechSynthesisUtterance(""))` 1회 + praise Audio 객체 `load()` 1회. 이후 비동기 재생 허용됨.
3. `visibilitychange`(숨김) 시 speechSynthesis.cancel(), 클립 pause.
4. 캐시: `english-reading.js?v=4`, `sw.js` CACHE_VERSION +1, praise mp3 9개 CORE_SHELL 프리캐시(오프라인 칭찬 유지).
5. 복습 기록(`readingMistake`)·오답 규칙·마이크 오류 처리는 지금 그대로. 이 스펙은 소리와 문구만 바꾼다.

## 4. 테스트 (cards/tests/hub-english-feedback.test.js 신설)

- 단계 매핑: 첫 시도 통과 → 1군 클립, 재시도 후 통과 → 2군, 3연속 → threeinarow. 실패·마이크 오류로 연속 0 리셋.
- 직전 클립 반복 없음(100회 난수 돌려 검증).
- 틀린 단어 낭독 목록: 문장 순서, 중복 제거, 최대 3개, wholeLine이면 빈 목록.
- speak 호출 전 recognizer.stop 호출됨(호출 순서 스파이).
- 영어 voice 없음 → speak 미호출, 오류 문구 없음.
- `?v=4` + CACHE_VERSION 핀(기존 pwa 테스트 패턴).

## 5. 검수 기준 (클로드)

- 아이패드: 통과 → 칭찬 소리 1.5초 내 시작, 화면 별과 동시. 실패 → 빨간 단어 소리 나고 나서야 다시 읽기 버튼 활성.
- 세 번 연속 통과 시 Three in a row 나오는지.
- 소리 나는 동안 마이크 꺼져 있는지(스피커 되먹임 오답 0건).

## 6. v2 변경 (2026-09-06 저녁, 클로드 직접 구현 — "음성인식이 한번씩 안 됨" + "칭찬이 끊김")

- **음성합성(speechSynthesis) 완전 제거.** 실패 후 틀린 단어는 `assets/study/words/<word>.mp3`(Gemini Leda, 38개, 288KB) 녹음 클립으로 읽어준다. iPad Safari에서 합성음 사용 뒤 마이크 세션이 간헐적으로 죽는 문제의 원인 후보를 없앤 것.
- **칭찬 길이 제한 제거.** 모든 칭찬 클립은 `ended`까지 재생. 5초 동안 재생 진행이 없을 때만 다음 문제로 넘어간다(정체 감시). 소리를 못 트는 환경은 1.8초 뒤 넘어감.
- **오디오 요소 1개를 페이지당 한 번만 해제**(첫 마이크 탭). 칭찬·단어 클립이 같은 요소를 돌려 쓴다.
- **인식 후보 5개**(`maxAlternatives = 5`). 첫 후보가 틀려도 다른 후보 하나를 바꿔 넣어 문장이 맞으면 통과. 화면 표시는 항상 첫 후보.
- **허용어 표(§5 커밋 2a08d33)** 유지: 제대로 읽었을 때 인식기가 돌려주는 오인식만 인정.
- **진단 로그**: 주소 뒤에 `?readinglog=1`을 붙이면 문장 아래에 이벤트 흐름(마이크 on/off, 결과 수, 오류 코드, 칭찬/단어 재생 결과)이 보인다. 인식 문장은 기록하지 않고 저장소에도 쓰지 않는다.
- 캐시: english-reading.js `?v=6`, 서비스 워커 v43, 단어 클립 38개 CORE_SHELL 프리캐시.

## 7. v3 변경 (2026-09-06 밤 — "같은 문장 반복", "칭찬 끊김·다음 문제 마이크 사망")

- **문장 16 → 68개.** 초1 어휘(색·동물·가족·동작·인사), 3~5단어. 새 단어 62개 클립 추가(`assets/study/words/`, 총 100개). 새 단어 허용어 표 확장.
- **최근 12문장 회피.** `chooseSentence(scores, ordinal, previous, random, recent)`: 최근 읽은 번호(허브 `hub2_reading_recent`, 이 기기만)와 직전 번호는 다른 문장이 남아 있는 한 다시 내지 않는다. 가중치 모드에서도 가중치 0. 복습 문제도 최근 12개 안의 문장은 건너뛴다.
- **오디오 요소는 문제마다 새로 생성**(첫 마이크 탭 안에서). 코덱스판에서 아이패드 검증된 패턴으로 되돌림. 페이지 공용 요소 1개 방식은 다음 문제 인식 사망 보고 후 폐기.
- **서비스 워커 자동 새로고침.** 배포 후 첫 방문에서 새 워커가 자리 잡으면(이전 워커가 있었고, 8초 안, 첫 설치 아님) 한 번 자동 새로고침 → "두 번 열기" 불필요. 이 변경 자체는 아직 옛 페이지에 없으므로 이번 배포만 두 번 열기.
- 캐시: english-reading.js `?v=7`, 서비스 워커 v44.

## 8. 읽기 단계 (2026-09-19, 클로드 직접 구현)

- 사용자 결정: 영어 읽기에 단계를 만든다.
- 1단계는 기존 68문장(3~6단어), 2단계 30문장(5~8단어 한 문장), 3단계 20문장(짧은 두 문장, 7~10단어). 기존 번호 0~67은 그대로라 오답노트·최근 기록이 안 깨진다.
- 그 단계 문장을 한 번에 맞힌 것이 20개 쌓이면 다음 단계로 오른다. 3단계가 끝. 내려가지는 않는다. 기기 저장 키 `hub2_reading_level`.
- 2단계 이상에서는 75%를 그 단계에서, 25%를 아래 단계 복습으로 낸다. 단계를 넘기지 않고 부르면 예전처럼 1단계만 나온다.
- 새 단어 32개 발음은 같은 Gemini TTS Leda 목소리로 만들어 `assets/study/words/`에 넣었다(총 132개). 모두 서비스 워커가 미리 저장한다.
- 대시보드 공부 줄에 `영어 N단계`를 보여 주고, 오르면 "영어 읽기 N단계로 올라갔어요!"를 띄운다.
- 한글 뜻은 읽기 전에는 숨기고, 맞게 읽으면 칭찬과 함께 보인다.

## 9. 8단계로 확장 (2026-09-19, 사용자 결정)

| 단계 | 이름 | 문장 수 | 모양 |
|---|---|---|---|
| 1 | 짧은 문장 | 63 | 3~4단어 |
| 2 | 조금 긴 문장 | 32 | 5~6단어 |
| 3 | 긴 문장 | 31 | 7~8단어 |
| 4 | 두 문장 | 30 | 짧은 두 문장 |
| 5 | 묻고 답하기 | 30 | 질문 + 대답 |
| 6 | 지난 일 말하기 | 30 | 과거형 한 문장 |
| 7 | 세 문장 이야기 | 30 | 11~16단어 |
| 8 | 그림책 한 쪽 | 30 | 네 문장, 17~21단어 |

- 기존 118문장은 번호(0~117)를 그대로 두고 단계만 다시 매겼다. 원래 1단계 중 5단어짜리 5개는 2단계로, 원래 2단계 중 7~8단어는 3단계로, 원래 3단계(두 문장)는 4단계로 옮겼다. 새 158문장은 뒤에 붙였다(총 276).
- 승급은 그 단계 문장을 한 번에 30번 맞힐 때. 복습은 바로 아래 두 단계에서만 25%.
- 학습 리포트(📖)에 영어 읽기 단계 선택 칸. 부모가 바꾸면 그 단계 진행 횟수는 0부터.
- 숫자 허용어 one→1, five→5, too→to/two/2 추가.
- 단어 발음 504개. 이번 372개 중 60개는 gemini-2.5-flash-preview-tts, 나머지는 gemini-3.1-flash-tts-preview로 10개씩 묶어 읽힌 뒤 침묵 구간에서 잘랐다(Leda 목소리). 잘린 수가 단어 수와 다르면 다시 요청했다. 표본은 gemini-flash-latest 받아쓰기로 확인했다.
- 주의: TTS 미리보기 모델은 하루 100회 제한이 있다(09-19에 2.5-flash-preview-tts 소진). 대량 생성은 묶음으로 한다.

## 소리 만들기 도구 (2026-09-25 Vertex로 옮김)

제미나이 API 크레딧이 끝나 단어 클립·그림책 낭독은 `tools/vertex-tts.py`로 만든다(Vertex `gemini-2.5-flash-tts`, us-central1, 목소리 Leda, ADC 인증). 규격은 전과 같다: 앞뒤 무음 제거, loudnorm -16, 48kHz 모노 mp3(단어 40k, 낭독 48k). 제미나이 API의 하루 100회 한도가 없고 크레딧 프로젝트로 청구된다.

- 단어: `python tools/vertex-tts.py word assets/study/words apple ball`
- 그림책: `python tools/vertex-tts.py page` (없는 쪽만)
- 확인: 새 apple 클립을 받아쓰기로 되돌려 "Apple" 확인(1.15초, 옛 클립 0.70초).
