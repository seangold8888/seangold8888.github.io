# 모험 상자 「어려운 길」 확장 스펙

작성: 2026-09-13 · 기획 클로드 / 구현 코덱스
근거: 재이가 오디세이 1장에서 **일반보다 어려운 길을 더 좋아한다.** 같은 수준의 도전을 다른 게임에도 준다.
선례: `odyssey/HARDMODE-SPEC.md` (구현 `00dff6e`, 밸런스 조정 `48eb25e`, 캐시 `d9ed149`)

---

## 0. 오디세이 어려운 길이 먹힌 이유 — 이 문서 전체의 공통 규칙

아래 여섯 가지는 **모든 게임에 똑같이** 적용한다. 게임별 절에서 다시 설명하지 않는다.

### 규칙 1. 속도·체력 배수만 올리지 않는다
배수만 키우면 반응속도 시험이 되고 자동 보정이 곧바로 되돌린다. 게임마다 **새 행동 하나** 또는 **새 판단 하나**를 반드시 넣는다.
오디세이에서는 「숙이기」였다. 천장의 손은 점프로 절대 못 피한다 — "기존 버튼 하나로 다 되는" 상태를 깨는 지점이 있어야 한다.

### 규칙 2. 의미 없는 하트를 눈에 보이는 압박 하나로 바꾼다
무제한 재시작이 붙은 하트 5개는 아이에게 아무 의미가 없었다. 실수가 **쌓이고**, 잘하면 **회복되고**, 그 상태가 **화면에서 보이는** 게이지 하나로 바꾼다.
오디세이에서는 「거인과의 거리」였고, 거리가 곧 거인의 화면 위치였다.

### 규칙 3. 회복에는 연성 상한을 둔다 (09-08 실측 교훈)
첫 구현에서 자동 회복이 게이지를 최대치에 붙여 버려 **위협이 화면에서 사라졌다.** 모든 압박 게이지는:
- 자동 회복은 시작값까지만 올린다(쉼터에서만 조금 더).
- 그 위로는 보상(별·처치·회피 등)으로만 올리고, 최대치에 가까울수록 보상 효과를 줄인다: `room = 1 - (값 - 상한) / (최대 - 상한)`.
- **깨끗하게 플레이해도 위협이 화면 가장자리에 계속 보여야 한다.** 이것을 완료 기준에 넣는다.

### 규칙 4. 게임 오버는 없다
0이 되면 기존 구조 연출(아테나·도움 마법 등)로 체크포인트 재시작. 3연속 실패 시 자동 보정은 **살려 두되 바닥값만 높인다**(오디세이 0.78 → 0.90).

### 규칙 5. 일반 모드는 한 프레임도 바뀌지 않는다
- 새 상수·분기는 전부 `HARD` 플래그 뒤에.
- 어려운 길은 **해당 장/코스를 일반으로 한 번 깬 뒤에만** 보인다. 깨기 전에는 자물쇠도 그리지 않는다.
- 저장은 기존 기록 옆에 `hardCleared`·최고 기록 필드를 **추가**만 한다. 옛 저장 자동 이관, 손상 JSON 안전.
- 어려운 길 클리어가 기존 해금·보상 계산을 바꾸지 않는다.
- 오디세이처럼 **일반 모드 N프레임 동일 비교 테스트**를 게임마다 넣는다.

### 규칙 6. 배포 체크 (09-08 사고 교훈)
JS/HTML을 바꾸면 **같은 커밋에서** `sw.js`의 `CACHE_VERSION`을 올리고, `cards/tests/pwa.test.js`·`cards/tests/hub-english-feedback.test.js`의 캐시 세대 핀을 같이 맞춘다. 안 올리면 설치된 아이패드는 옛 페이지를 계속 쓴다.

### 공통 표기
- 진입 버튼 라벨: `🔥 어려운 길`, 깬 뒤 `🔥 어려운 길 ✓` (오디세이와 동일).
- 화면 낭독: 압박 게이지가 위험 구간에 **처음** 들어갈 때 한 번만.
- 음소거 존중, `prefers-reduced-motion`이면 흔들림 생략.

### 공통 밸런스 검증
오디세이 검수 때 쓴 방식을 게임마다 재사용한다(계측은 `page.route` 응답에만 주입, 배포 소스에 넣지 않음).

| 시나리오 | 기대 |
|---|---|
| 깨끗한 플레이 | 구조 0회, 압박 게이지가 **최대치에 붙지 않음**, 위협이 화면에 보임 |
| 실수 드문드문 (장 전체에 3~4회) | 구조 0회 |
| 실수 몰아서 (무적 풀리자마자 연속) | 3~4회째에 구조 |
| 아무것도 안 함 | 장 중반 이전에 구조 |

---

## 1. 대상과 순서

| 순서 | 게임 | 적합도 | 이유 |
|---|---|---|---|
| **H1** | 호그와트 마법학교 (`hogwarts/`) | 매우 높음 | 오디세이와 **같은 골격**(자동 보정 0.82·구조·체크포인트·별 등급). 6개 학년 |
| **H2** | 케데헌 (`kedehun/`) | 높음 | 액션 플랫포머. 이미 `JUST DODGE`가 있어 새 판단을 붙이기 쉽다 |
| **H3** | 산리오 카트 3D (`kart3d/`) + 간단판 (`kart/`) | 높음 | 지금은 고무줄이 **앞선 AI를 0.72배까지 늦춰** 사실상 질 수 없다 |
| **H4** | 삼국지·서유기·수호지 (`sanguo/`) | 중간 | 「사지」 난이도가 이미 있으나 배수만 올린다(규칙 1 위반) |
| **H5** | 헬로키티의 리본방 (`bori/`) | 중간 | 퍼즐. 액션식 압박 대신 추론 규칙을 한 단계 올린다 |

**이번 범위 밖**
- **재이와 태오의 멀티버스** (`avengers/`): 배포본은 1.4MB 빌드 번들이고 원본이 `OneDrive\문서\jay-teo-multiverse`에 있다. 빌드가 OneDrive 폴더에 `dist`를 쓰면 실시간 동기화가 돈다(디스크 사고 전례). 게다가 이미 `full`/`junior` 조작 프로필이 난이도 역할을 한다. 별건으로 다룬다.
- **J&T Adventure** (`cards/`): 코덱스 확장 작업 진행 중. 겹치지 않게 제외.
- 공주 옷장·이야기 극장·수학: 난이도 구조가 아니다.

---

## H1. 호그와트 마법학교 — 「어려운 학년」

### H1.1 현재 골격 (실측)

6개 학년이 같은 틀을 공유한다. 각 장 함수: `BroomRescue`(411) · `MovingStairs`(471) · `PotionClass`(729) · `Quidditch`(1050) · `RequireRoom`(1325) · `CastleDefense`(1647).

| 요소 | 현재 |
|---|---|
| 조작 | 4방향 이동 + `cast`(길게 누르면 연사) + `nova`(별 5개로 2초 보호) |
| 체력 | `p.hp = hero.hp` (해리·헤르미온느 5, 론 6) |
| 실패 | 0이면 `rescue` → 체크포인트로 되감기, 체력 회복 |
| 자동 보정 | 3회 실패 시 `assist = .82` 고정 (428, 534, 779, 1098, 1382) |
| 보스 | `state='vulnerable'` 창이 열리면 주문 (605, 873, 1171) |
| 등급 | `reportClear` 별 1~3 (피격·수집 기준) |
| 저장 | `wq_save` v2, `chapters[id] = {unlocked, cleared, bestStars}` (`loadSave` 1957) |

### H1.2 새 판단 — 「방패 주문」 (전 학년 공통)

규칙 1의 "기존 버튼으로 다 되는 상태를 깨는 지점"은 **튕겨 내야만 하는 공격**이다.

- 새 입력 `shield`: 키보드 `Shift`/`K`, 터치는 기존 주문 버튼 **왼쪽**에 「🛡️ 방패」. 누르는 순간부터 `0.35초` 동안 방패. 재사용 `0.9초`.
- 어려운 학년에만 **금빛 테두리 투사체**가 섞인다. 금빛 투사체는
  - 주문으로 **파괴되지 않는다**(맞혀도 통과).
  - 방패로 막으면 **반사**되어 가장 가까운 적에게 날아가고, 별 조각 1개를 준다.
  - 이동으로 피할 수는 있다(오디세이 손과 달리 완전 봉쇄는 아니다 — 비행 게임이라 좌표가 자유롭다). 대신 **반사 보상**이 압박 게이지 회복의 주 수단이라 피하기만 하면 게이지가 줄어든다.
- 발사 전 `0.8초` 금빛 조준선(기존 경고 연출 재사용)과 `S.warning` 3틱.
- 학년별 금빛 투사체의 주인:

| 학년 | 주인 | 비고 |
|---|---|---|
| 1 금지된 숲 | 큰 픽시(`kind:'big'`, `shot:1.5`)의 3발 중 1발 | 이미 발사 로직이 있다 |
| 2 움직이는 계단 | 날아오는 책 4권 중 1권 | `books` 스폰(557) |
| 3 마법약 교실 | 떨어지는 재료 중 「끓어넘친 방울」 | `drops` 스폰(793), 받으면 안 되는 것 |
| 4 퀴디치 | 블러저 3개 중 1개 | `bludgers`(1107) |
| 5 필요의 방 | 먼지 유령이 뱉는 먼지 뭉치 | `dusts`(1391) |
| 6 호그와트 방어전 | 심술 구름의 번개 | 코덱스가 스폰 위치 실측 후 지정 |

- 보스전: 보스의 매 라운드 마지막 공격을 금빛으로. **반사로 맞히면 `vulnerable` 창이 바로 열린다.** 방패가 보스 공략의 지름길이 된다.

### H1.3 압박 게이지 — 「별빛」

하트 대신 `glow`(0~100). 호그와트 이야기의 공통 적은 "별빛이 어두워지는 것"이라 세계관과 맞는다.

```
GLOW = { max:100, start:60, soft:60, softRest:75,
         regen:2.5,    // 초당, 상한까지만 (구현 실측으로 5.5 → 2.5, 아래 H1.7)
         regenDelay:3, // 맞은 뒤 3초는 회복하지 않는다 (구현 추가)
         hit:-24,      // 피격
         reflect:+9,   // 금빛 반사 (상한 위에서는 room 배율)
         star:+1.0,    // 별 조각 (상한 위에서는 room 배율)
         nova:-0,      // 수호 마법은 게이지를 쓰지 않는다
         checkpoint:68 }
```

- **보이는 위협**: `glow`가 낮을수록 화면 가장자리에서 어둠이 조여 온다. 반경 = `lerp(화면 대각선, 260px, 1 - glow/100)`. `glow = 60`(연성 상한)에서도 가장자리 **12%는 어둡게** 보여야 한다(규칙 3).
- 30 미만: 어둠 가장자리에 보라색 맥동, 3.5초마다 낮은 경고음.
- 0: 기존 `rescue` 그대로. 재시작 시 `glow = start`.
- 5학년 「필요의 방」은 이미 `lightR`(랜턴 반경 170)이 시야를 쥐고 있다. 여기서는 어둠 반경 대신 **`lightR`를 `glow`에 묶는다**: `lightR = 120 + glow * 0.9`. 자동 보정 +40은 하드에서 +20.

### H1.4 하드 전용 숫자

| 항목 | 위치 | 일반 | 하드 |
|---|---|---|---|
| 자동 보정 | 428 외 | `.82` | `.90` |
| 1학년 스크롤 | 434 `targetSpeed` | 기준 | ×1.10 |
| 1학년 픽시 속도 | 422 `90+ph*10` | 기준 | `100+ph*12` |
| 2학년 책 간격 | 561 | `4.25/2.95/3.35` | ×0.82 |
| 3학년 낙하 속도 | 793 | `96/116/126` | ×1.12 |
| 4학년 블러저 간격 | 1111 | `4.4/3.2/3.6` | ×0.82 |
| 보스 공략 창 | 605·873·1171 | `[1.5,1.3,1.12]` 등 | ×0.80, **단 반사 성공 시 창 +0.4초** |
| `nova` 필요 별 | `charge<5` | 5 | 7 |
| 무적 시간 | `p.inv` 피격 후 | 1.25 | 1.0 |

### H1.5 해금·저장

- 해금: `chapters[id].cleared === true`인 학년에만 「🔥 어려운 학년」. 학년 선택 화면의 해당 학년 카드 아래.
- 저장 v3: `chapters[id]`에 `hardCleared:boolean`, `hardBestStars:0~3` **추가**. `loadSave`(1957)가 v2를 그대로 읽어 기본값을 채운다.
- 어려운 학년 클리어는 **다음 학년 해금에 영향 없음**.
- 별 등급(하드): 3별 = 구조 0회 & 반사 6회 이상, 2별 = 구조 1회 이하, 1별 = 클리어.

### H1.6 완료 기준

1. 일반 6학년 N프레임 동일(계측 비교).
2. 깨지 않은 학년에는 버튼이 없다.
3. 금빛 투사체는 주문으로 안 사라지고, 방패로 막으면 반사되어 적을 맞힌다.
4. `glow = 60`에서 화면 가장자리가 어둡게 보인다(스크린샷 첨부).
5. 보스 금빛 공격 반사 → 공략 창 즉시 열림.
6. 방패 버튼과 이동·주문을 **동시에** 눌러도 서로 입력을 훔치지 않는다(오디세이 `pointerId` 규칙).
7. 공통 밸런스 검증표 4줄 통과(학년 1·4·6은 필수, 나머지는 스모크).
8. `wq_save` v2 → v3 이관, 손상 JSON 안전.

### H1.7 구현 메모 (2026-09-13, 클로드 구현)

- **회복 수치 변경 (실측).** 스펙의 `regen 5.5`는 드문드문 맞는 아이에게 일반 모드보다 **쉬웠다.** 일정 간격으로 맞을 때 구조까지 걸리는 피격 수:

  | 회복·대기 | 6초마다 | 8초마다 | 10초마다 | 12초마다 | 15초마다 | 연달아 |
  |---|---|---|---|---|---|---|
  | 5.5 · 0초 (스펙 원안) | 10대 | 구조 없음 | 없음 | 없음 | 없음 | 4대 |
  | **2.5 · 3초 (채택)** | **4대** | **5대** | 7대 | 없음 | 없음 | **3대** |
  | 일반 하트 | 5대 | 5대 | 5대 | 5대 | 5대 | 5대 |

  8초 간격까지는 일반보다 쉽지 않고, 한 장에 3~4번 띄워 실수하면 버틴다. 이 표는 `hogwarts/tests/hard-mode.cjs`의 밸런스 가드로 고정했다.
- **3학년 금빛 공격은 재료가 아니라 연기.** 재료는 몸으로 받는 것이 정답이라 "닿으면 아프다"와 충돌한다. 초록 연기 3개 중 1개, 솥이 뱉는 연기 두 번에 한 번이 금빛이다.
- **5학년 보스방.** 장롱은 공격하지 않으므로 어려운 학년에서만 보스방에도 먼지 유령이 나온다. 금빛 먼지를 반사하면 숨은 더미의 `tell` 단계를 건너뛰고 바로 공략 창이 열린다.
- **3별 조건의 반사 수 6 → 4.** 계단·연기처럼 금빛이 드문 학년에서 6은 사실상 불가능했다.
- **아무것도 안 할 때 구조 시점.** 원안 수치에서는 1학년이 보스(63초)에 가서야 구조됐다. 채택 수치에서는 **32초**(숲 중반)에 구조된다. 4학년 1구간은 가만히 있는 자리를 블러저 네 줄 중 한 줄만 지나가서 60초 동안 구조되지 않는다 — 지형 문제라 수치로 억지로 맞추지 않았다. 6학년 2구간은 20초.
- **공통 모듈** `WQ.Hard`(`hogwarts/index.html`의 `src/hard.js` 블록). 일반 모드에서는 모든 함수가 항등이라 장 코드에 호출을 넣어도 동작이 같다. 이전 커밋과 여섯 학년을 같은 난수·입력으로 돌려 60프레임마다 화면 해시를 비교해 **전부 동일**함을 확인했다.

---

## H2. 케데헌 — 「혼문 무대」

### H2.1 현재 골격 (실측, `kedehun/index.html`)

| 요소 | 현재 |
|---|---|
| 스테이지 | `STAGES`(546) 2개 — 코랄 시티 아레나(보스 그림자 사자, 380) · 달빛 옥상(사자 보이즈 진우, 520) |
| 조작 | 이동·점프·공격 3연타(`COMBO_STEPS` 584)·대시·궁극기 (키: `Space/↑`, `X/J`, `K/Z`, `C/L`, `Shift`) |
| 체력 | `MAX_HEALTH = 120`(582), 피해 `damage × 0.65`(1646), 피격 무적 `1.6`(1648) (`hurtPlayer` 1629) |
| 회피 | 대시(`p.dash = 0.16`, 1216) 중 피격 → `JUST DODGE!` 에너지 +8 (1631~1643) |
| 실패 | 체력 0 → `respawnTimer 1.35` → 체크포인트 재시작 (1659, `loadStage` 1062) |
| 기록 | `starlight-rescue-dx-best`(590) |

### H2.2 새 판단 — 「혼문 파열」은 대시로만 넘는다

오디세이 「천장의 손」과 같은 역할이다.

- 어려운 무대에서 **사자(`saja`)와 보스**가 붉은 원형 경고 `0.9초` 뒤 「혼문 파열」을 쓴다.
  - 막기·점프로 피할 수 없다(점프 높이 전체를 덮는 기둥).
  - **대시로 관통**하면 `JUST DODGE`가 뜨고, 하드에서는 추가로 `0.35초` 슬로모 + 사자가 `1.2초` 경직(반격 기회).
- 일반 적(`shade`·`wisp`·`brute`)은 쓰지 않는다. 판단이 필요한 순간을 사자·보스로 한정해 리듬을 만든다.
- 첫 파열은 **시범**: 맞아도 피해 없음 + 힌트 「빨간 원이 보이면 대시로 뚫고 지나가!」.

### H2.3 압박 게이지 — 「무대 열기」

케이팝 공연이라는 세계관 그대로. 관객 함성이 식으면 조명이 꺼진다.

```
HYPE = { max:100, start:55, soft:55, softRest:70,
         regen:4.0,       // 초당, 상한까지만
         hit:-22,         // 피격 (체력 대신)
         kill:+6, heavyKill:+10, justDodge:+12, shard:+1.5,  // 상한 위에서는 room 배율
         checkpoint:65 }
```

- 하드에서는 `health` 대신 `hype`가 실패를 결정한다. `hurtPlayer`의 체력 감소를 `hype` 감소로 바꾸고, 0이면 기존 `respawnTimer` 경로.
- **보이는 위협**: 배경 무대 조명 밝기 = `hype`. 55(연성 상한)에서도 조명 두 줄 중 **한 줄은 꺼져 있다**. 30 미만이면 관객 실루엣이 흐려지고 스포트라이트가 흔들린다.
- HUD 체력 막대 자리를 「무대 열기」 막대로 교체(색 + 관객 아이콘 수로 이중 표기).

### H2.4 하드 전용 숫자

| 항목 | 위치 | 일반 | 하드 |
|---|---|---|---|
| 피해 배율 | `hurtPlayer` 1646 | `0.65` | `0.9` (게이지 환산은 `hit` 고정값 사용) |
| 피격 무적 | 1648 | `1.6` | `1.1` |
| 보스 체력 | `STAGES[].bossHealth` | 380 / 520 | 460 / 640 |
| 적 배치 | `STAGES[].enemies` | 기준 | 체크포인트 이후 `shade` 1기 추가 |
| 대시 재사용 | 대시 로직 1215~1221 | 기준 | 기준 유지 (대시가 핵심 행동이므로 **늘리지 않는다**) |

### H2.5 해금·저장·완료 기준

- 해금: 스테이지를 일반으로 깬 뒤 스테이지 선택에 「🔥 혼문 무대」. 기존 `easy-gate.test.js`의 보스 게이트 통과 계약은 하드에서도 유지.
- 저장: 새 키 `kedehun-hard-v1 = { stages: { 0:{cleared,bestScore}, 1:{...} } }`. 기존 `BEST_KEY`는 건드리지 않는다.
- 완료 기준: 일반 N프레임 동일 / 혼문 파열은 점프로 못 피하고 대시로만 통과 / 시범 파열 무해 / `hype 55`에서 조명 한 줄 꺼짐(스크린샷) / 세 수호자(`lumi`·`mira`·`joy`) 전원 하드 완주 / 공통 밸런스 검증표 통과.

### H2.6 구현 메모 (2026-09-13, 클로드 구현)

- **해금 단위를 스테이지에서 캠페인으로.** 케데헌에는 스테이지 선택이 없고 1→2스테이지가 이어지는 캠페인뿐이다. 그래서 **일반 1스테이지를 한 번 깨면** 출격 버튼 옆에 「🔥 혼문 무대」가 열리고, 누르면 두 스테이지를 어려운 규칙으로 달린다(키보드 `H`). 이 기능 전에 이미 끝까지 깬 기기는 최고 기록(`starlight-rescue-dx-best`)을 근거로 바로 열린다.
- **기록 분리.** `kedehun-hard-v1 = {normalStage1, normalWon, hardStage1, hardWon, hardBest}`. 혼문 무대 점수는 일반 최고 기록을 덮지 않는다.
- **혼문 파열의 형태.** "붉은 원 뒤 기둥"을 **화면 높이 전체를 덮고 900px/초로 밀려오는 붉은 기둥**으로 구현했다. 제자리 기둥이면 걸어서 벗어날 수 있어 대시 판단이 생기지 않는다. 점프로 못 넘고 걸어서 못 벗어나며, **기둥 쪽으로 대시해 관통**해야 피한다. 관통하면 기존 JUST DODGE 연출 + 함성 +12 + 멈춤 0.2초 + 시전자 1.2초 경직("관통! 빈틈"). 전조 문구는 `combat-v2.js` `intent`에 유형 4로 추가(`?v=2`).
- **빈도.** 사자 보이즈는 공격 세 번에 한 번, 보스는 네 번에 한 번. 두 번에 한 번(보스)은 자동 플레이 두 종류 모두 300초 안에 보스를 못 잡아 **전투가 멈췄다.** 현재 값에서 파열을 대시로 뚫는 자동 플레이는 93초·구조 0회, 연타만 하는 자동 플레이는 100초·구조 1회로 끝난다 — 대시가 보상이지 벽이 되지 않는다.
- **함성 수치는 호그와트 실측값을 그대로.** 회복 2.5/초, 맞은 뒤 3초 대기, 피격 −22, 시작·연성 상한 55. 연달아 3대, 6초마다 4대 이내, 8초마다 5대 이내에 구조되고 15초 간격이면 버틴다(`kedehun/tests/hard-mode.test.js`). 일반 모드는 체력 120·피해 ×0.65라 같은 상황에서 열 대 넘게 버틴다.
- **보이는 위협.** 배경 그림이 불러와지면 원래 조명 효과가 그려지지 않으므로, 화면 효과 층에 무대 조명 세 줄을 따로 그린다. 함성 45~89에서 두 줄, 15~44에서 한 줄, 그 아래는 꺼지고, 식은 만큼 가장자리가 어두워진다(30 미만은 붉게 맥동).
- **검증.** 이전 커밋과 현재 파일로 **일반 캠페인 전체(1·2스테이지, 승리까지 58초)**를 같은 난수·입력으로 돌려 30프레임마다 전투 상태가 전부 같음을 확인했다. 기존 케데헌 테스트 27개 통과.

---

## H3. 산리오 카트 3D + 간단판 — 「라이벌 레이스」

### H3.1 현재 골격 (실측)

| 요소 | 카트 3D (`kart3d/src`) | 간단판 (`kart/src`) |
|---|---|---|
| 모드 | 아이템 배틀·스피드 매치(AI 3)·타임어택 (`game.js` 13) | 단일 레이스 |
| 바퀴 | 6코스 모두 3바퀴 (`tracks.js`) | `LAPS = 2` (`main.js` 7) |
| 고무줄 | `karts.js` 584~585: 앞서면 **최저 0.72배**까지 감속, 뒤지면 최대 1.05 | `kart.js` 220~221: 900 앞서면 0.84, 900 뒤지면 1.06 |
| 기본 속도 | `mult 0.965` (완벽하게 안 몬다) | `1 - 0.34 × curve` |
| 보상 | 최고 랩 | 도전 배지 ★3 (`main.js` 23~31) |

**문제**: 3D에서 AI가 앞서면 0.72배까지 떨어진다. 아이가 멈춰 있지 않는 한 결국 따라잡힌다. 이기는 기분은 있지만 **이긴 이유가 없다.**

### H3.2 새 판단 — 라이벌 한 명과 「아이템 지키기」

- 하드에서는 AI 3명 중 **한 명이 라이벌**(코스별 고정: 쿠로미·배드바츠마루 등 캐릭터 목록에서 코덱스가 지정). 이름표 위에 🔥.
- 라이벌만 다르게 운전한다:
  - 고무줄 **감속 없음**(앞서도 `mult 0.965` 유지), 뒤질 때 따라잡기는 최대 1.03.
  - 아이템을 **바로 쓰지 않는다.** 방패·바나나를 쥐고 있다가 플레이어가 **뒤 1.2초 이내**로 붙으면 뒤로 던진다. 플레이어가 앞서 있으면 폭탄을 앞으로 쏜다.
- 그래서 플레이어에게 판단이 생긴다: **방패를 먹었을 때 바로 쓸지, 라이벌 뒤에 붙기 전까지 쥐고 있을지.** 쥐고 있으면 라이벌의 바나나를 막을 수 있다.
- 나머지 AI 2명은 일반 고무줄 그대로(판을 붐비게 만드는 역할).
- 스피드 매치(아이템 없음) 하드: 라이벌은 드리프트 부스트를 코너마다 쓴다(`drift` 반환값을 코너에서 확정). 판단 = 드리프트를 길게 잡을지.

### H3.3 압박 게이지 — 「라이벌과의 간격」

- 화면 상단에 라이벌 얼굴 아이콘 + `+1.4초` / `-0.8초` 간격 표시. 트랙 미니 진행바 위에 두 점(나·라이벌).
- **보이는 위협**: 간격이 1초 이내면 라이벌 카트가 **백미러 창**(화면 오른쪽 위 작은 원)에 비친다. 뒤에 있을 때도 보이게 해서 "쫓기는 느낌"을 준다.
- 카트는 게임 오버가 없고 등수로 끝나므로 규칙 2·3은 이렇게 옮긴다: 라이벌 AI의 기본 속도 자체가 연성 상한이다 — **라이벌이 너무 멀리 도망가지 않도록** 뒤처진 플레이어와 3초 이상 벌어지면 라이벌 최고 속도를 `0.94배`로 낮춘다(그래도 앞서서 감속하는 일반 AI의 0.72보다 훨씬 강하다).

### H3.4 해금·보정·저장

- 해금: 해당 코스를 일반 **1등**으로 끝낸 뒤 코스 카드에 「🔥 라이벌 레이스」.
- 규칙 4 보정: 같은 코스 하드에서 **3연속 라이벌에게 지면** 라이벌 최고 속도 −3%(최대 −6%까지). 힌트 「라이벌 바나나는 방패로 막을 수 있어!」.
- 저장: 3D `sanrio-kart3d:hard:<trackId> = {won:boolean, bestLap}` · 간단판 `sanrio-kart:hard:<trackId> = {won:boolean}`. 기존 키 불변.
- 우승 보상: 코스 카드에 🔥 배지. 기존 도전 배지 ★ 계산에 넣지 않는다.

### H3.5 완료 기준

1. 일반 모드 AI 궤적 N프레임 동일(라이벌 로직이 일반 AI에 새지 않음).
2. 라이벌은 앞서도 감속하지 않는다(시뮬: 플레이어 정지 → 라이벌이 계속 멀어짐 → 3초 넘으면 0.94배).
3. 라이벌 바나나를 방패로 막을 수 있다. 방패 없으면 스핀.
4. 플레이어가 드리프트 부스트를 코너마다 쓰면 하드 우승 가능, 아무 기술 없이 달리면 **라이벌에게 진다**(헤드리스 자동 운전으로 두 경우 모두 확인).
5. 간단판·3D 동일 규칙.

---

## H4. 삼국지·서유기·수호지 — 「사지」 재설계

### H4.1 현재 (실측, `sanguo/src/game/difficulty.js`)

`사지(hard)` = 플레이어 체력 0.92 · 적 체력 1.10 · 적 공격력 1.28 · 병력 1.14 · 동시 공격 최대 4 · 쿨다운 0.86 · 분노 0.88.
**전부 배수다(규칙 1 위반).** 다섯 버튼(공격·활·필살기·돌진기·승마)과 `counter`(3.2초 쿨다운·620ms 판정, `sideScroller.js` 1777~1783)가 이미 있는데 난이도가 그 버튼들을 요구하지 않는다.

### H4.2 새 판단 — 「파훼 공격」은 반격이나 돌진기로만

- 사지에서 **부장(`captain`)과 적장**이 붉은 테두리 강공 「파훼 공격」을 쓴다. 준비 `0.75초`, 적 몸이 붉게 빛나고 경고 배너.
  - 일반 공격으로 끊을 수 없다(슈퍼아머).
  - **`counter` 입력이 준비 창 마지막 0.3초 안에** 들어오면 튕겨 내고 적 `1.4초` 경직.
  - **돌진기**로 적을 **관통**해도 회피(오디세이 숙이기·케데헌 대시와 같은 관통 판단).
  - 뒤로 걸어서 사거리 밖으로 나가도 피할 수 있다. 대신 반격·관통 성공 시에만 보상.
- 이미 수련·출진·사지 선택이 있으므로 해금 조건은 추가하지 않는다(기존 동작 유지). 대신 사지 첫 진입 시 30초짜리 「파훼 연습」 파도를 한 번 보여 준다(저장 `vesper-sgz:hard-tutorial:v1`).

### H4.3 압박 게이지 — 「군기」

- 사지에서만 HUD에 `morale`(0~100). 체력은 그대로 두고, `morale`이 **아군 지원**을 결정한다.
- 반격 성공 +10 · 돌진 관통 +8 · K.O. +2(상한 위 room 배율) · 피격 −8 · 파훼 공격 피격 −18. 자동 회복 없음 → 대신 연성 상한 개념을 거꾸로: **50 아래로는 초당 +2로 서서히 복귀**(바닥 보정).
- `morale ≥ 70`: 화면 뒤에 아군 깃발 부대가 보이고 파도 사이 체력 +10%.
- `morale < 30`: 적 궁수 지원이 1명 늘고 배경 깃발이 적 색으로 바뀐다(보이는 위협).
- 체력 0은 기존 패배 흐름 그대로.

### H4.4 배수 되돌리기

파훼 공격이 난이도를 담당하므로 순수 배수는 낮춘다: 적 공격력 `1.28 → 1.12`, 적 체력 `1.10 → 1.00`. 병력·동시 공격 상한은 유지.

### H4.5 완료 기준

1. 수련·출진 동작 불변(`combat-playthrough.cjs` 15건 그대로 통과).
2. 파훼 공격은 일반 공격으로 끊기지 않고, 창 안의 `counter`·돌진 관통으로만 튕겨진다.
3. `combat-playthrough.cjs`에 사지 3건 추가(관우·조운·손오공) — 스크립트가 반격을 쓰면 7파도 클리어, 반격 없이 버티기만 하면 `morale`이 30 아래로 떨어지는 것까지 확인.
4. 터치 5버튼 배치 불변(`browser-controls.cjs`).

---

## H5. 헬로키티의 리본방 — 「비밀 미스터리」

### H5.1 현재 (실측, `bori/index.html`)

4장소 + 피날레: 리본 규칙(패턴 306) · 구름 카페(주문 326) · 꽃 정원(미로 338) · 파티룸(자리 단서 355) · 리본 상자(보석 순서 366). 변형 3종(`variant`), 힌트 3단계(`HINTS` 371) — **3단계에서 정답 칸이 빛난다**(`getAssist` 473). 저장 `bori-gems:v3:progress`.

퍼즐 게임이라 규칙 2의 "보이는 위협"은 액션식이 아니다. 대신 **리본 3개**를 쓴다.

### H5.2 새 판단 — 장소마다 규칙 하나 추가

| 장소 | 일반 | 비밀 미스터리 |
|---|---|---|
| 리본 규칙 | 빈칸 1개 | **빈칸 2개**, 반복 단위 4까지(예: 하트·별·별·달) |
| 구름 카페 | 주문서가 계속 보임 | 주문서를 **5초 보여 주고 접는다.** 「다시 보기」는 리본 1개를 쓴다 |
| 꽃 정원 | 입구 → 보석 | **열쇠 칸을 먼저 밟아야** 보석 문이 열린다(두 단계 경로) |
| 파티룸 | "누구 옆에 앉아요" 긍정 단서 | **부정 단서 1개 섞기**("쿠로미는 창가가 아니에요") |
| 리본 상자 | 보석 순서 단서 | 단서 하나가 **다른 단서를 가리킨다**("달은 별보다 먼저, 별은 세 번째") |

- 퍼즐 데이터는 기존 `PATTERNS`/`ORDERS`/`MAZES`/`PARTY`/`FINALE` 옆에 `HARD_*` 배열로 **추가**, 변형 3종씩.
- 문장은 초1이 소리 내어 읽을 수 있는 길이(한 단서 20자 안팎).

### H5.3 압박 — 「리본 3개」

- 장소마다 리본 3개. 틀리면 1개, 카페 「다시 보기」도 1개.
- 0개 → 그 장소만 **새 변형으로 처음부터**(게임 오버 없음, 모은 보석 유지).
- 힌트: 하드에서는 2단계까지만. **3단계 정답 빛내기(`getAssist`) 없음.** 대신 같은 장소를 2번 다시 시작하면 3단계가 풀린다(규칙 4 바닥 보정).
- 리본은 화면 위에 실제 리본 모양으로. 1개 남으면 헬로키티가 걱정하는 표정.

### H5.4 해금·저장·완료 기준

- 해금: 피날레 클리어 후 시작 화면에 「🔥 비밀 미스터리」.
- 저장: `bori-gems:v3:progress`에 `hard:{completed:[], bestRibbons:{room:0..3,...}}` 추가. v3 이관 안전.
- 완료 기준: 일반 퍼즐 데이터·흐름 불변 / 하드에서 `getAssist`가 첫 두 번의 시도 동안 `null` / 리본 0 → 같은 장소 새 변형 재시작, 다른 장소 진행 유지 / 모든 `HARD_*` 퍼즐이 **유일한 정답**을 갖는지 전수 검사 테스트(자동 풀이기로 해 개수 = 1).

---

## 6. 마일스톤

| M | 내용 | 산출물 |
|---|---|---|
| **H1** | 호그와트 6학년 어려운 학년 | 방패·금빛 투사체·별빛 게이지·저장 v3·스모크 테스트·밸런스 표 |
| **H2** | 케데헌 혼문 무대 | 혼문 파열·무대 열기·저장·스모크 |
| **H3** | 카트 3D + 간단판 라이벌 레이스 | 라이벌 AI·백미러·자동 운전 2경우 테스트 |
| **H4** | 삼국지 사지 재설계 | 파훼 공격·군기·플레이스루 3건 |
| **H5** | 리본방 비밀 미스터리 | HARD 퍼즐 15종(5장소×3변형)·유일해 검사 |

**H1이 끝나면 멈추고 재이 실플레이.** 호그와트가 오디세이만큼 반응이 좋으면 H2~H5를 같은 수치 틀로 진행하고, 방패가 초1에게 어려우면(동시 입력 부담) 금빛 투사체를 "피하기만 해도 되는" 쪽으로 낮춘 뒤 진행한다.

각 M의 커밋마다 규칙 6(캐시 세대 + 테스트 핀)을 지킨다. 검수는 클로드가 오디세이 때처럼 계측 스크립트로 공통 밸런스 표를 실측해서 한다.
