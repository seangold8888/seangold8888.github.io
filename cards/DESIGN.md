# 이야기 카드 배틀 — 게임 설계서 v1

> 작성: 클로드(기획) → 구현: 코덱스. 이 문서가 단일 기준(source of truth).
> 카드 스탯 데이터는 `cards.json`, 미술 지시는 `ART_DIRECTION.md` 참조.

## 0. 제품 한 줄

퍼블릭 도메인 명작 캐릭터로 싸우는 초등 저학년용 카드 배틀.
포켓몬 TCG 골격(HP·기술·약점·동전) + 하스스톤 마나(별사탕 자동 램프).
오디오북(`/story/`)을 끝까지 들으면 카드가 해금된다.

## 1. 대상·환경

- 플레이어: 초1 (김재이). 글은 읽지만 긴 문장은 부담 → 아이콘+숫자 중심 UI.
- 기기: 아이패드 Safari 가로 모드 우선, 홈 화면 앱 실행. 데스크톱은 덤.
- 경로: `site/cards/` → `seangold8888.github.io/cards/`
- 허브 등록: v1 완성 후 index.html 게임 목록에 카드 추가 (수학 관문 안쪽, 다른 게임과 동일).

## 2. v1 범위 (이것만 만든다)

- **1:1 맞대결**: 내 카드 1장 vs 컴퓨터 카드 1장. 대기석·교체·소환 없음.
- 시작 전 내가 해금 카드 중 1장 선택. 컴퓨터는 대표 v1 카드 중 내 카드와 레어도 차이가 ±1 이내인 상대를 랜덤 선택한다. 범위 안의 다른 카드가 하나도 없으면 높은 레어도 상대 대신 같은 카드 미러전을 사용한다.
- 승리 = 상대 카드 기절(HP 0).
- 별사탕: 두 쪽 다 턴 시작에 +1 (최대 5 저금). 소환이 없으므로 v1에서 별사탕은 기술 비용 전용.
- 동전 던지기: 화면 중앙 큰 동전 애니메이션. 내 기술 판정은 아이가 직접 탭하고, 컴퓨터 판정은 자동 회전하되 결과를 공격보다 먼저 보여 준다.
- 기절 시: 승리 팡파르 + "다음 이야기를 들으면 새 친구가 온다" 안내로 /story/ 유도.
- **v2로 미루는 것**: 대기석 2장, 교체, 소환 비용, 덱 8장, 아빠 대전(핫시트), 진화.

## 3. 턴 구조 (고정 3단계)

```
① 별사탕 +1 (최대 5)
② 기술 1개 선택 (비용 지불 가능한 것만 활성화)
③ 데미지 적용 → 상대 턴
```

- 기술 비용을 낼 수 없으면 "쉬기" 버튼(별사탕 모으기)만 활성.
- 약점 타입 공격이면 데미지 ×2, 화면에 "×2!" 크게 표시.
- 컴퓨터 AI: 낼 수 있는 기술 중 데미지 기대값 최대를 선택. 단 마무리 가능하면 무조건 마무리. 그 외 30% 확률로 두 번째 좋은 수(아이가 이길 여지).

## 4. 데이터 계약

- `cards.json` 스키마:

```json
{
  "id": "cinderella",
  "name": "신데렐라",
  "emoji": "👠",
  "type": "wise",            // brave | wise | magic | monster
  "rarity": 2,               // 1 일반 ⭐ / 2 레어 ⭐⭐ / 3 전설 ⭐⭐⭐
  "summonCost": 2,           // v1 미사용, v2 대비
  "hp": 60,
  "passive": { "name": "유리 구두", "desc": "...", "fx": "revive_half_once" },
  "attacks": [
    { "name": "호박마차 돌진", "cost": 2, "dmg": 30, "fx": null, "desc": "" }
  ],
  "unlock": "cinderella",    // /story/ 에피소드 id. null = 기본 지급
  "art": "art/cinderella.png"
}
```

- `fx` 코드 (v1 구현 대상만):
  - `revive_half_once` 기절 시 1회 HP 절반 부활
  - `reduce_dmg_10` / `reduce_dmg_20_monster` 받는 데미지 감소
  - `coin_evade` 피격 시 동전 앞면 = 회피
  - `coin_miss` 공격 시 동전 뒷면 = 빗나감
  - `dmg_half_enemy_hp` 데미지 = 상대 남은 HP 절반(내림, 10단위)
  - `dmg_stack_10` 사용마다 +10 누적
  - `skip_next_enemy` 상대 다음 턴 기술 금지 (동전 조건은 `coin_` 접두)
  - `gain_star_1` / `steal_star_1` 별사탕 획득/훔침
  - `heal_40` 자기 HP 40 회복 (v1: 대상=자신)
  - `self_hurt_10_eot` 턴 종료마다 자기 HP -10
  - `first_hit_zero` 처음 받는 공격 무효
  - `wish_limit_3` 판당 기술 사용 총 3회 제한
  - 나머지 fx(벤치 관련)는 v2 — 효과를 무시한 채 버튼만 노출하는 반쪽 구현은 금지한다. v1 기술이 하나도 없는 카드는 v1 풀에서 제외하고, v1 기술이 남는 혼합 카드는 v2 기술만 숨긴다.
  - 요정 대모는 「별가루 톡!」과 「호박 마법」이 완전한 v1 기술이므로 포함하며, 대상이 없는 부활 기술만 v2까지 숨긴다. 눈의 여왕도 「얼음 거울」이 완전한 v1 기술이므로 포함한다. 브레멘 음악대·손오공·과자집 마녀·아서왕·콩나무 거인은 v1 기술 보강 전까지 제외한다.
  - 로딩 시 card.v1이 false가 아니며 v1 가용 기술이 1개 이상인 카드만 풀에 넣도록 검증한다.

## 5. 해금 연동 (이미 작동 중인 것에 붙이기)

- `/story/` 플레이어가 완청 시 `localStorage["story_done_<episodeId>"] = "1"` 기록 중.
- 카드 해금 판정: `card.unlock === null || localStorage["story_done_" + card.unlock] === "1"`.
- 같은 도메인(github.io)이라 localStorage 공유됨. 경로 무관.
- 잠긴 카드는 컬렉션 화면에 실루엣+자물쇠로 표시, 탭하면 "「○○」 이야기를 끝까지 들으면 만날 수 있어!" + /story/ 링크.
- 전체 컬렉션은 cards.json의 collection 목록에 적힌 24장이다. 첫 6편 완청 기준으로는 기본 2장에 cinderella(신데렐라·요정 대모), odyssey_cyclops(오디세우스·폴리페모스), threepigs(아기돼지 삼형제), tortoisehare(토끼와 거북), redhood_story(커다란 늑대), jack_story(콩나무 거인)가 더해져 10장이 활성화된다. 나머지도 각 이야기의 `story_done_*` 기록으로 같은 방식으로 깨어난다.
- 아기돼지 삼형제·토끼와 거북·커다란 늑대는 해금 즉시 v1 1:1 대전에 출전 가능하다. 콩나무 거인은 해금·수집 대상이지만 v1: false이며, 컬렉션에서 “대전 준비 중”으로 표시하고 선택·대전할 수 없게 한다.
- 최소 시작 보장: 아무것도 해금 안 됐으면 빨간 모자·콩나무 잭 2장 기본 지급(unlock: null로 지정).

## 6. UI 원칙 (사용자 확정 사항 — 위반 금지)

1. **이미지는 캐릭터 그림만.** 카드 이름·HP·기술명·숫자·별·타입 아이콘은 전부 HTML/CSS/SVG 텍스트 레이어. 한글 선명도 + 밸런스 숫자 즉시 수정 가능해야 함.
2. 카드 = DOM 컴포넌트. `cards.json` 수정 → 새로고침만으로 반영.
3. 비주얼 톤 = 허브 「밤하늘 오락실」 계열 (`--night #201a40`, `--star #ffd93d`, `--paper #fff6e6`, 두꺼운 테두리 + 아래 그림자 버튼). 세부는 ART_DIRECTION.md.
4. 아이패드 터치 규격: 탭 타깃 최소 60px, `-webkit-user-select:none`, `touch-action` 관리, 고무줄 스크롤 차단 — 기존 게임들 head 패턴 복사.
5. 소리: 타격·동전·승리 효과음은 WebAudio 합성(외부 에셋 금지). 음소거 토글.

## 7. 검수 기준 (5단계에서 클로드가 볼 것)

- 원작 반영도: 능력이 원작 장면과 연결되는가, 미술 지시서의 금지사항(디즈니 요소) 준수.
- 재미: 초1이 3턴 안에 "다음에 뭘 할지" 스스로 결정 가능한가. 한 판 3분 이내.
- 숫자 검산: 모든 HP·데미지 10의 배수, 약점 ×2 포함 오버킬 밸런스.

## 8. v1.5 업데이트 지시 (2026-08-31, 사용자 피드백 반영)

### 8a. 반영 완료 (클로드)
- 컬렉션 정렬: 깨어난 카드 → 깨울 수 있는 대전 카드 → 수집 전용 순. 묶음 내 원래 순서 유지 (app.js shelfOrder).
- 기술 다양화 1단계: 모든 v1 카드가 기술 2개 이상 (cards.json — 잽/유틸 추가, 기존 fx 코드만 사용, 엔진 무수정). "1별 잽을 쓸까, 모아서 한 방을 칠까"의 매턴 선택지 생성.

### 8b. 코덱스 구현 몫 ① — 타격음 임팩트 (사용자: "타격음이 아직 부족")
- 문제: 타격이 "맞았다"는 쾌감 부족. 방향 = 트랜지언트와 저역.
- 어택 3ms 이내 순간 피크(현재 shape 어택이 부드러워 뭉개짐), 60~110Hz 사인 바디를 지금의 1.5~2배 게인으로, 소프트 클리핑(waveshaper)으로 눌러 따뜻하게.
- 강타(약점 ×2)는 일반타와 확실히 구분: 저역 2배 길게 + 노이즈 크랙 + 화면 흔들림(CSS)과 동기.
- 타격 순간 룸센드는 줄이고(잔향이 임팩트를 흐림) 꼬리에만 잔향.
- 음량 상한은 유지 (아이패드 스피커, 초1 청력 보호).

### 8c. 코덱스 구현 몫 ② — 「이야기의 조각」 시스템 (사용자: "공격이 단순, 머리 쓰게")
매턴 "제일 센 기술 누르기"를 넘어서는 의사결정 레이어. 벤치(v2)보다 작은 공사로 깊이 추가.

- 대결 시작 시, 내가 **깨운 카드들의 이야기에서 조각 3개**가 무작위로 손패에 들어온다 (컴퓨터도 동일 규칙).
- 조각은 **판당 각 1회용**, 내 턴에 기술 대신이 아니라 **기술과 함께** 1개 사용 가능 (턴 구조: 별사탕 → 조각(선택) → 기술).
- 조각 효과는 원작 소품에서 (기존 fx 재사용 가능한 것 우선):
  - 유리 구두(신데렐라): 이번 턴 받는 데미지 0 (coin 없이 확정 회피 1회)
  - 호박 마차: 이번 기술 데미지 +20
  - 벽돌 한 장(아기돼지): 다음 받는 데미지 -30
  - 떡 하나(해와 달): 별사탕 +2
  - 황금 거위 알(잭): HP +30
  - 여의봉 조각(손오공): 이번 기술 비용 -1
  - 참기름(해와 달): 상대 다음 기술 동전 앞면이어야 명중
  - 대동강 물 한 바가지(봉이 김선달, 추후): 상대 별사탕 1개를 내 것으로
- 깨운 이야기가 많을수록 조각 풀이 커진다 → 듣기 루프 강화. 최소 풀(이야기 2개)일 때도 작동해야 함.
- UI: 손패 3장을 화면 하단 기술 바 위에 작은 칩으로. 탭 = 미리보기, 다시 탭 = 사용. 사용한 조각은 뒤집힘.
- AI: 조각 사용 규칙 단순하게 — 마무리 가능하면 강화 조각, HP 절반 이하면 방어/회복 조각, 그 외 30% 랜덤.
- 테스트: 조각 효과 각각 + "조각 없이도 게임 성립" 경로.

### 8d. 검수 기준 추가
- 초1 기준: 조각 이름·아이콘만 보고 효과가 연상되는가.
- 한 판 여전히 3분 이내인가 (조각 고민으로 5분 넘어가면 실패).

### 8e. 캐시 규칙 (코덱스 공통)
- index.html의 스크립트 태그는 `?v=N` 버전 쿼리를 단다. JS를 고치면 반드시 N을 올린다 — 아이패드 사파리가 예전 스크립트를 붙들고 있는 사고가 실제로 났음.

### 8f. 코덱스 구현 몫 ③ — 공격 비주얼 이펙트 (사용자: "영상 이펙트도")
소리와 눈이 같은 순간에 터져야 타격감이 완성된다. 8b 사운드와 반드시 동기.
- 타입별 이펙트, 카드 아트 위에 오버레이 (CSS/Canvas, 외부 에셋 금지):
  - ⚔️ 용기: 사선 베기 궤적 + 불꽃 파편
  - 📘 지혜: 화살/책장 회오리 + 푸른 잔상
  - ✨ 마법: 별가루 폭발 + 보라 링 확산
  - 🌑 괴물: 화면 전체 쿵 흔들림 + 흙먼지
- 공통: 피격 카드 흰색 플래시 1프레임 + 밀려남(translateX 스프링), 데미지 숫자가 크게 떠올랐다 사라짐(약점 ×2는 금색+2배 크기)
- 기절: 카드가 회색으로 기울며 별이 흩어짐. 부활(유리 구두): 금빛 기둥.
- 성능: 아이패드 60fps 유지, transform/opacity만 애니메이션. `prefers-reduced-motion` 존중.
- 검수 기준: 이펙트 총 길이 0.6초 이내(턴 템포 유지), 3연타로 봐도 눈 안 아플 것.

### 8g. 코덱스 구현 몫 ④ — 오프라인 지원 (PWA)
사용자 요구: 인터넷 없이 플레이. 사이트 전체(허브+이야기 극장+카드+기존 게임)를 서비스 워커로 오프라인화.
- `site/sw.js` 하나로 통합, 허브·story·cards 각 index에서 등록.
- 전략:
  - HTML/JS/CSS/JSON: 캐시 우선 + 백그라운드 갱신(stale-while-revalidate). 버전 키는 `?v=` 규칙과 연동.
  - 카드 아트(webp/png): 캐시 우선, 설치 시 프리캐시 24장 webp(~수 MB).
  - 오디오북 mp3(총 ~15MB): **재생한 편만** 러닝 캐시(전체 프리캐시는 첫 로딩을 해침). 단 "모두 저장" 버튼을 이야기 극장에 추가 — 누르면 전편 프리캐시(여행 전 준비용).
- manifest.json: 이름 「모험 상자」, 아이콘 기존 icon.png, display standalone — 이미 홈 화면 앱 메타는 있으니 정합만.
- 캐시 버전: `adventure-box-v1` 형식, JS 수정 시 버전 올려 구캐시 삭제.
- Range 요청 주의: 사파리 오디오는 Range로 받아감 — mp3는 캐시 응답이 Range를 지원하도록 처리(전체 blob 응답이면 대부분 동작하나 실기 검증 필수).
- 검수 기준: 아이패드 비행기 모드에서 ① 허브 열림 ② 카드 대결 1판 ③ "모두 저장" 후 오디오북 재생 — 3개 다 통과.

## 9. v1.6 업데이트 지시 — 기술별 연출 + 배경음악 (사용자 피드백)

사용자 원문: "조약돌 던지기 하면 실제로 빨간모자가 조약돌을 던져서 상대방이 맞는 효과, 사냥꾼을 부르면 사냥꾼이 소환되어 상대방을 공격하는 효과가 나와야 재밌지."

### 9a. 데이터 (클로드 반영 완료)
- cards.json 모든 기술에 `vfx: {kind, emoji, big?}` 추가됨. kind 6종:
  - `projectile`: 시전 카드에서 이모지가 포물선으로 날아가 상대 카드에 명중 (조약돌🪨, 화살🏹, 유리구두👠, 서리바늘❄️)
  - `summon`: 조력자 이모지가 시전자 옆에 뿅 나타나 상대에게 달려들어 치고 퇴장 (사냥꾼🪓, 트로이목마🐴, 참새🐦, 양떼🐑)
  - `strike`: 시전 카드가 직접 돌진/타격 (기존 is-acting 확장)
  - `burst`: 상대 카드 위에서 이모지 폭발 (열두 과업💥, 어흥🐯)
  - `aura`: 시전자 주위 상승 이펙트 (회복·강화, 황금알🥚, 별가루)
  - `debuff`: 상대 카드에 하강/감싸는 이펙트 (돌로 만들기🗿, 노려보기👀)
- `big: true` = 고코스트 피니셔. 크기 1.5배 + 화면 흔들림 + 강타 사운드.

### 9b. 코덱스 구현 몫 — 연출 엔진
- 타사 게임 추출물·외부 네트워크 에셋은 금지한다. 저장소에 포함된 자체 제작 수채·금박·잉크 VFX 텍스처는 허용하며, 이모지는 자산이 없는 기술의 폴백으로만 사용한다.
- projectile: 시전 카드 중심→상대 카드 중심 포물선 450ms, 명중 순간 기존 피격 연출(플래시+밀림+데미지 숫자)과 사운드 동기. **빗나감(coin_miss)이면 포물선이 상대 카드를 스쳐 지나가 화면 밖으로** — 이게 웃김 포인트.
- summon: 등장(스케일 0→1, 200ms) → 상대에게 대시 → 타격 → 반대편으로 퇴장. 총 800ms 이내.
- 회피(coin_evade): 상대 카드가 옆으로 쓱 비켜서 projectile/strike가 허공을 침.
- 조각 사용 시에도 해당 조각 이모지 aura 연출.
- 성능: 이모지 span은 풀링(생성 재사용), transform/opacity만, reduced-motion 시 즉시 결과 표시.

### 9c. 코덱스 구현 몫 — 배경음악 (WebAudio 생성, 외부 에셋 금지)
- 컬렉션 화면: 오르골 자장가풍 8마디 루프 (기존 pluck/bell 재사용, 펜타토닉, BPM 66, 볼륨 아주 낮게 0.02~0.03).
- 배틀 화면: 조용한 모험풍 루프 — 낮은 현 드론 + 가벼운 목금 리듬, BPM 92. 내 HP 30% 이하일 때 반음 올려 긴장감.
- 승리/패배 시 BGM 정지 후 스팅어.
- 루프는 lookahead 스케줄러(setTimeout+currentTime)로 이음새 없이. 탭 백그라운드 전환 시 정지.
- 음소거 버튼이 BGM까지 포함. BGM만 끄는 작은 토글(♪) 추가 — 오디오북 들으며 카드하는 경우 대비.
- 검수 기준: 10분 틀어놔도 안 지겨울 것(변주 2개 이상 랜덤 교차), 아이패드 스피커에서 효과음을 안 가릴 것.

## 10. 통합 품질 기준 (2026-08-31 사용자 승인 마스터 프롬프트)

사용자가 확정한 완성 기준. 이후 모든 작업은 이 절을 만족해야 한다.
원문 요지: "처음 보는 사람이 '진짜 판매되는 카드게임 같다'고 느낄 시각적 완성도 + 3분 안에 다시 하고 싶은 전략성."

- 레퍼런스 해석: 하스스톤=타격감·자원관리·읽히는 전장, 포켓몬=캐릭터 카드 문법·수집. 단 프레임·UI배치·아이콘·로고·색체계·사운드·애니메이션은 복제 금지. 「별빛 이야기 극장」 독자 언어로.
- 비주얼: 일러스트가 주인공. 프레임 = 별빛 동화책·인형극·보석 결합 독창형. 타입별 실루엣 구분(용기=따뜻한 금속+주황 불꽃 / 지혜=청색 잉크+책장 회오리 / 마법=보라 별가루+빛 고리 / 괴물=짙은 흙빛+묵직한 먼지). 레어도=장식 밀도와 빛, 가독성 우선. 선택 시 입체 기울기+광택 이동. 밤에 봐도 안 무섭게.
- 사운드: '벅' 금지, 타입별 재질감, 3ms 피크+안전 저역+소프트 클리핑 (§8b·§9c와 동일).
- UI: iPad 가로·세로+모바일, 터치 60px+, 아이콘 중심, 실루엣 폴백, reduced-motion·음소거.
- 완료 기준: 24장 정상표시 / 한 판 완주 / 기술·조각·AI·부활·약점·기절 테스트 / iPad 양방향 무결 / 비행기모드 3종 통과 / 콘솔 오류 0 / 미구현 기능 표시 금지.

### 10a. 가드레일 (검수자 클로드 — 위반 시 반려)
1. **기존 24장 일러스트 재생성 금지.** 이미 검수 통과한 자산. 아래 공통 프롬프트는 신규 카드(예: 봉이 김선달)와 부득이한 교체 승인 건에만 사용.
2. **§9c 배경음악 포함.** 이 마스터 프롬프트에 BGM이 명시돼 있지 않으나 §9c는 유효한 지시다.
3. 프레임 디자인 참고: 디자인 캔버스 「금박 고서」안(남색+금박 괘선+명조 이름, 레어도=동/은/금 테) — 같은 방향의 선행 시안. 그대로 쓰라는 게 아니라 출발점.
4. 한 판 3분·초1 가독 원칙은 어떤 연출보다 우선한다. 연출이 템포를 잡아먹으면 연출을 줄인다.

### 10b. 신규 카드 일러스트 공통 프롬프트 (영문, 이미지 생성용)
Create an original premium fantasy storybook character illustration for a collectible card game.
Character: [이름] / Scene and action: [장면] / Expression: [표정] / Signature props: [소품] / Character type: [용기/지혜/마법/괴물]
Art direction: original cinematic children's fantasy illustration; hand-painted storybook finish with sculpted, readable shapes; spectacular lighting and rich material detail; strong silhouette readable at small card size; energetic diagonal composition; expressive, child-friendly face; clear foreground character and atmospheric background; magical particles matching the character type; premium trading-card quality; exciting and wondrous, never frightening; leave breathing room around the head and main action; no text, numbers, logos, UI, watermark or card frame.
Avoid: copyrighted character likenesses; recognizable Disney, Pokémon or Hearthstone costume and frame designs; adult glamour styling; horror, exposed teeth, blood, weapons aimed at the viewer; photorealistic adult facial proportions; extra fingers, malformed hands, duplicate props.

## 11. v1.7 전투 가독성·방어·이야기 필살기 (2026-09-02 사용자 확정)

### 11a. 읽히는 대형 공격 연출
- §8f의 `0.6초 이내` 제한은 이번 사용자 피드백으로 대체한다. 기술 종류별 총 길이는 **0.72~0.96초**, 접촉은 0.27~0.64초에 발생한다.
- 공격 궤적·포털·베기·링·섬광·잔광 범위를 기존보다 약 1.45배 확대한다. 프리미엄 원화 VFX는 124~340px, 이모지 폴백은 일반 1.5배·강타 2.1배다.
- 느려지는 부분은 예고/이동 구간이다. 피격 플래시·링·히트스톱은 0.24~0.34초로 짧게 유지하고, 소리와 시각 접촉은 같은 프레임에 실행한다.
- 확대 후에도 Canvas 파티클 풀 120개, DPR 1.5 상한, transform/opacity 중심, reduced-motion 계약을 유지한다.

### 11b. 공용 방어 행동 + 상대 행동 예고
- 모든 v1 대전 카드는 `🛡️ 방어하기`를 사용할 수 있다. 비용은 별사탕 1개이며 턴을 종료한다.
- 다음 상대 공격의 최종 양의 피해를 최대 20 줄이되, 최소 10 피해는 남긴다. 첫 피격 무효·완전 방어 조각·감소 조각을 먼저 계산하여 강한 1회용 방어가 낭비되지 않게 한다.
- 실제 피해를 줄였을 때만 방어막을 소비한다. 상대가 공격하지 않으면 다음 내 턴 시작에 만료된다.
- 방어를 사용한 바로 다음 내 턴에는 재정비 때문에 다시 방어할 수 없고, 그다음 내 턴부터 다시 사용할 수 있다. 방어만 반복하는 교착을 막으면서 상대 예고를 읽는 선택은 유지한다.
- 플레이어 턴에는 상대의 예약 행동(기술명·예상 피해·동전/조각·강타/치명 여부)을 미리 보여 준다. 플레이어의 별 훔치기·약화 등으로 예약 행동이 불가능해진 경우에만 AI가 합법 행동을 다시 계산한다.
- AI는 방어해야만 다음 치명타를 생존할 수 있으면 방어를 우선하고, 체력 절반 이하에서는 유효 방어를 30% 확률로 선택한다.

### 11c. 완청 연동 「이야기 관문」 필살기
- `localStorage["story_done_<episodeId>"] === "1"`인 이야기만 문제를 열 수 있다. 기본 카드 잭·빨간 모자도 각각 `jack_story`·`redhood_story` 완청이 필요하다.
- 문제는 카드 표면의 수치가 아니라 이야기 속 사건을 묻는 초1용 3지선다다. 정답이면 턴·HP·별사탕을 쓰지 않고 즉시 필살기를 해금한다.
- 첫 오답은 다음 내 턴에 한 번 더 답할 수 있다. 두 번째 오답이면 그 판의 관문만 닫힌다. 오답으로 HP·별·턴을 빼앗지 않는다.
- 필살기 `🌟 별빛 이야기`: 판당 1회, 별사탕 3개, 중립 기본 피해 50. 약점 ×2·공격자 강화·데미지 조각은 적용하지 않고 동전 없이 확정 명중한다. 상대의 영구 감소·방어 조각·첫 피격 무효·공용 방어는 정상 적용한다.
- v1에서는 플레이어 전용 보상이다. 컴퓨터는 문제를 푼 것으로 간주하거나 필살기를 자동 해금하지 않는다.
- 문항 데이터는 `cards/js/story-gates.js`에 두어 `cards.json` 24장/48기술/검수 완료 원화를 변경하지 않는다.

### 11d. 검수
- 방어 전후 피해·만료·우선순위, AI 생존 방어, 관문 2회 제한, 필살기 중립 50/판당 1회/동전 무시를 자동 테스트한다.
- 상대 예고와 방어 버튼은 iPad와 모바일에서 동시에 읽혀야 하며, 이야기 문제 dialog는 카드 잠금 dialog와 독립이어야 한다.

## 12. v1.8 이야기 관문 확장 (2026-09-03, 사용자 피드백 "하나의 퀴즈가 반복돼")

- 문항 은행: 카드당 1문항 → **5~6문항**(총 100+), `js/story-gates.js`. 유형을 섞음 — 사건·인물·소품·이유·숫자·대사. 정답은 오디오 v2 대본 기준.
- 반복 방지: `getForCard(card, rng, {avoidIds})` + app.js가 카드별 최근 3문항을 `localStorage["cards_quiz_recent_<cardId>"]`에 기억. 같은 카드로 연달아 대결해도 같은 문제가 안 나옴.
- 오디오 v2: 20편 전부 확장(총 334세그먼트, 편당 +40%). 퀴즈에 쓸 사건(소 이름 밀키화이트, 콩 다섯 알, 히드라 아홉 머리, 팍톨로스 강, 세 노파의 눈 하나…)을 추가. 에피소드 id·파일명은 그대로라 완청 기록·해금 유지.
- 캐시 v24. (참고: tests/pwa.test.js 「Avengers migrates…」는 이 변경 이전부터 실패 — 어벤져스 index.html 마이그레이션 순서 검사, 코덱스 확인 필요.)
